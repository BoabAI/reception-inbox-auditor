import branding from "../../../branding.json";

export const brand = branding;
export const colors = branding.colors;
export const font = branding.font;
export const style = branding.style;
export const video = branding.video;
